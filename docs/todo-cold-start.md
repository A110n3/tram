# 任务清单：修复"首次使用需加载模型，浮窗一直提示加载中"

> 背景：2026-08-18 用户报告 OCR 识图 / 划词翻译第一次触发时，后端本地模型
> 未加载，浮窗提示"（等待后端响应，可能在加载模型）"后长时间不变，用户以为
> 卡死，需重新识图/取词才能用。会话额度有限，每完成一步在下方打勾并记录。

## 诊断结论（已完成，2026-08-18）

- [x] 纯后端管线无 bug：`tools/repro_slow_first_request.py` hold /
      headers_then_hold 模式（延迟 15s）均正常拿到结果。
- [x] 真实后端为 Lemonade Server（RyzenAI，localhost:13305）。复现：
      `curl -X POST /api/v1/unload` 卸载模型后走应用真实管线
      （`tools/repro_cold_start_real.py`）：
  - 模型未加载时首个请求 **阻塞到加载完成才返回**（实测 TTFB≈41s，
    NPU 首次冷编译可能数分钟），期间无任何字节 -> 浮窗 8s 慢提示出现
    后一直停留 -> 与用户"卡死"感受一致。加载完成后本次请求其实会成功，
    但用户早已重新触发（此时模型已热，立刻成功）。
  - 另一种服务器状态下未加载直接返回 500（1ms），现有重试逻辑可自愈。
  - 次要发现：Qwen3 为思考模型，加载后首批 delta 是 `reasoning_content`
    （我们已正确忽略只取 `content`）；但若思考耗尽 `max_tokens`，
    翻译会"成功"但结果为空字符串 -> 浮窗被清空，无任何提示。
- [x] 根因定性：不是管线 bug，是**首个请求承担了模型加载成本** + 
      **空结果无守卫**。修复方向：启动预热 + 空结果守卫。

## 修复步骤

- [x] **S1 建立本任务清单**（本文件）。
- [x] **S2 启动预热（核心修复）**：
      - `worker.py`：`TestConnectionWorker` 增加 `timeout` 参数（默认 30s
        不变，预热传配置的 timeout=180s，等待模型加载完成）。
      - `main_window.py`：新增 `_start_warmup()`，应用启动后、设置保存
        （rebuild_backend）后触发；结果静默（成功不打扰、失败留给真实
        翻译按正常管线报错）；退出时取消（quit_app 内）。
      - 效果：模型加载发生在托盘启动阶段，用户首次热键翻译时模型已热。
- [x] **S3 空结果守卫**：`translator.py` `_translate_chunk` 中
      `chat_stream` 正常返回但拼接结果为空时，视为可重试错误
      （BackendError 无状态码 -> 走既有重试/退避），3 次全空后报
      "模型返回空结果（思考可能耗尽 max_tokens）"，不再静默清空浮窗。
- [x] **S4 测试**：
      - `tests/test_core.py` 新增空结果用例：先空后正常 -> 重试后成功；
        恒为空 -> 最终失败且报错文案正确。
      - `tests/test_worker.py` 回归通过。
- [x] **S5 清理**：删除一次性脚本 `tools/repro_cold_start_real.py`
      （诊断已完成）；保留 `tools/repro_slow_first_request.py` 供复验。
- [x] **S6 提交**：单 commit，含本清单与代码修复。

## 遗留（可选，下次额度再做）

- [ ] L1 慢提示文案升级：浮窗慢提示 8s 后出现一次就不变，可改为
      显示已等待秒数（QTimer 更新），或附"后端正在加载模型，请稍候"。
- [ ] L2 Lemonade 专用：利用 `/api/v1/load` 显式加载模型（比发请求预热
      更标准），失败再回退到请求预热。仅对 Lemonade 后端生效。
- [ ] L3 浮窗"加载中"期间提供手动取消外的重试按钮（当前只能关掉重按热键）。

## 记录

- 2026-08-18：完成全部诊断（见上）。
- 2026-08-18：S1-S6 完成。验证：96 个测试全过；卸载模型后
  TestConnectionWorker(timeout=180) 预热冷模型 9.3s 成功返回。
  `tools/repro_slow_first_request.py` 保留供回归复验。
- 2026-08-18：遗留 L1-L3 见上，额度恢复后可继续。
